# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres-service
# spec:
#   type: NodePort
#   selector:
#     app: postgres
#   ports:
#     - protocol: TCP  # Protocol used by the service (TCP in this case)
#       port: 5432
#       targetPort: 5432
#       nodePort: 30432  # You can now connect to Postgres from localhost:30432
